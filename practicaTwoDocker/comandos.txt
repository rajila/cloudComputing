# Continuacion del ejercicio practicaOneDocker
docker swarm init

docker stack deploy -c docker-compose.yml ponepythonservice

# Muestra los servicios desplegados
docker stack ls

docker service ls

docker service ps ponepythonservice_web

#Remover los servicios
docker stack rm ponepythonservice

#Logout Docker swarm
docker swarm leave --force